# ✈️ 프로젝트 작업 히스토리 (Travel Planner)

## 1. 프로젝트 주요 마일스톤

### 기반 구축
- [x] **2026-04-13:** 프로젝트 초기 PRD 작성 및 기술 스택(Python/Streamlit) 확정.
- [x] 브랜치 전략 도입: `main`(배포용) / `dev`(개발용) 분리.
- [x] Streamlit Community Cloud 배포: https://travel-planner-0i.streamlit.app/

### 보안
- [x] Google Cloud 서비스 계정(Service Account) 연동 및 `.streamlit/secrets.toml`을 통한 비공개 DB 구축.
- [x] `start.sh`(원클릭 실행) 및 `fix_secrets.py`(보안 키 자동 변환) 제작.
- [x] `fix_secrets.py` 고도화: 비밀번호 및 독스 ID 자동 포함.
- [x] 로그인 시스템 도입: `st.secrets` 기반 비밀번호 인증.

### 여행 준비 탭
- [x] 예산 관리 기능: 항목별 입력, 구글 시트 실시간 반영.
- [x] 숙소 예산 폼 고도화: 체크인/아웃 → 박수·총금액 자동계산, 무료 취소 기한, 메모 필드 추가.
- [x] 체크리스트 기능 구현.
- [x] 예상일정 섹션 추가: 구글 시트 `상세일정` 탭 연동. 확정 체크박스 클릭 시 `ok`로 시트 자동 저장.

### 여행 현지 탭
- [x] 현지 탭 지도 구현: 1일차~N일차 버튼, 확정 일정만 번호 핀 표시, 핀 순서 연결선(점선), 동일 좌표 핀 오프셋 처리.
- [x] 구글 지도 링크 → 좌표 자동 추출: 단축 링크(maps.app.goo.gl) 리다이렉트 추적 후 lat/lon + 장소명 시트에 자동 입력 (11개 완료).
- [x] 일정 목록 소요시간·이동수단 추가: 구글 시트 `소요시간`, `이동수단` 컬럼 연동.
- [x] 일정 목록 HTML 테이블 전환: 메모 줄바꿈(\n → `<br>`) 지원.

### UI / 모바일 최적화
- [x] 동기화 버튼 상단 이동: 사이드바 → 타이틀 우측.
- [x] Streamlit 기본 헤더 숨기기, 제목과 버튼 한 줄 배치, st.info 슬림화.
- [x] 모바일 폰트 위계 설정: h1 25px, h2 19px, 본문 14.5px.
- [x] thinklog 헤딩 CSS 소형화: h1 1.2rem / h2 1.05rem / h3 0.95rem.

### AI 여행 비서 탭
- [x] 구글 독스 thinklog 연동: Gemini API 할당량 문제로 앱 내 AI 채팅 제거 → 독스 뷰어 + 외부 링크 방식으로 전환.

### 성능
- [x] 데이터 session_state 캐시: 일차 버튼 클릭 시 시트 재조회 없이 빠르게 전환.
- [x] `ttl=0` → session_state 캐시 방식으로 전환.

---

## 2. 해결된 주요 이슈

| 이슈 | 원인 | 해결 |
|---|---|---|
| 맥 환경 설정 | 경로 문제 | venv + start.sh 쉘 스크립트로 해결 |
| 보안 이슈 | 링크 공유 방식 | 서비스 계정 전용 이메일 편집자 권한으로 전환 |
| Gemini API 할당량 | 결제 활성화 시 무료 할당량 0 | 외부 AI 링크로 대체 |
| TOML 파싱 오류 | 최상단 키가 `[connections.gsheets]` 하위로 인식 | 섹션 위로 이동 |
| 일차 버튼 전체 초기화 | 버튼 클릭 시 매번 시트 재조회 | session_state 캐시로 해결 |
| NaN 표시 | pandas 빈 셀 NaN | `pd.isna()` 체크로 해결 |
| 메모 줄바꿈 미표시 | `st.dataframe` 미지원 | HTML 테이블 + `<br>` 변환 |
| thinklog 헤딩 과대 | Streamlit 기본 h1/h2 크기 | `.thinklog` CSS 클래스로 소형화 |
| 모바일 폰트 엉망 | `span`, `p` 전역 선택자가 Streamlit 내부 요소까지 덮어씀 | `.stMarkdown p, label`로 범위 축소, thinklog CSS에 `!important` 추가 |

---

## 3. 파일 목록 및 역할
- `app.py`: 메인 웹 애플리케이션 (예산, 체크리스트, 예상일정, 현지 지도, thinklog 뷰어).
- `start.sh`: 맥 로컬 실행 스크립트 (venv 자동 생성 포함).
- `fix_secrets.py`: 구글 서비스 계정 JSON 키 → secrets.toml 자동 변환 도구.
- `.streamlit/secrets.toml`: 구글 시트 서비스 계정 키, 독스 ID, 비밀번호 보관소 (비공개, 깃 제외).

---

## 4. 브랜치 전략 및 작업 방식
- **기본 원칙:** 모든 신규 기능 개발 및 버그 수정은 `dev` 브랜치에서 진행.
- **배포 프로세스:** `dev`에서 충분히 검증된 작업물만 `main` 브랜치로 머지하여 최종 배포.
- **커밋 관리:** 의미 있는 단위로 커밋하며, `main`은 항상 실행 가능한 최신 배포 상태 유지.

---

## 5. 다음 과제
- 구글 지도 링크 추가 시 좌표 자동 추출 자동화 (앱 내 처리)
- 현지 탭: VND 경비 입력 및 KRW 자동 환산
- 영수증 OCR (AI API 환경 구성 후)

---
*마지막 업데이트: 2026년 4월 13일*
